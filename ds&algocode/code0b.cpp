// 2.Check if subarray with 0 sum is exists or not

#include<bits/stdc++.h>
using namespace std;

void check(vector<int> arr1){
    
}

int main(){
    vector<int> arr;
    int n;
    cin>>n;
    for(int i=0; i<n; i++){
        int x;
        cin>>x;
        arr.push_back(x);
    }
    check(arr);
    return 0;
}